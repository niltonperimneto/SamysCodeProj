@{
  ViewData["Title"] = ":egg:";
}

<div class="hero min-h-[50vh] bg-base-200 rounded-box mt-10 shadow-xl">
  <div class="hero-content text-center">
    <div class="max-w-md">
      <h1 class="text-5xl font-bold text-primary">howdyyyyy!!!!</h1>
      <p class="py-6 text-xl">
        ok so this is the SECOND site i'm making since te first one went to shit whoopsies'
      </p>
    </div>
  </div>
</div>

<a class="btn btn-secondary shadow-md fixed bottom-4 right-4 z-50 rounded-full hover:scale-105 transition-transform"
  asp-controller="Home" asp-action="Privacy">
  ?
</a>